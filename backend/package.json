{
  "name": "waviate-backend",
  "version": "1.0.0",
  "description": "Backend server for Waviate WhatsApp Outreach Platform",
  "main": "server/server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server/server.js",
    "build": "echo 'No build step required'",
    "dev": "nodemon server/server.js",
    "test": "jest",
    "debug": "ls -la && find . -type f | sort && node -e \"console.log('Environment:', process.env)\" && node server/server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.38.4",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "axios": "^1.6.2",
    "morgan": "^1.10.0",
    "twilio": "^4.19.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "jest": "^29.7.0"
  }
} 